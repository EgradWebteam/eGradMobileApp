import { SafeAreaView } from "react-native-safe-area-context"
import { View } from "react-native-web"
import { Text } from 'react-native';
const Profile=()=>{
    return(
        <SafeAreaView>
            <Text>This is the prfile page!!!!!!!!!!!!!</Text>
        </SafeAreaView>
    )
}
export default Profile